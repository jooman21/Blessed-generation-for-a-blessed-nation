PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ngo_website
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Secret
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=1d

# For production, add additional environment variables
# such as email service credentials, payment gateway keys, etc.
